<%= form_for(@website_history) do |f| %>
  <% if @website_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@website_history.errors.count, "error") %> prohibited this website_history from being saved:</h2>

      <ul>
      <% @website_history.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :website_id %><br>
    <%= f.text_field :website_id %>
  </div>
  <div class="field">
    <%= f.label :success %><br>
    <%= f.check_box :success %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
